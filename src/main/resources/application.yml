server:
  port: 8080

# 禁用启动时的横幅输出
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://nas.zsqnas.icu:3306/big_event?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345678

  data:
    redis:
      host: nas.zsqnas.icu
      port: 6379
      # password: 123456   # 如无密码可省略
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  main:
    banner-mode: off

mybatis-plus:
  global-config:
    banner: false
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: zsqReallyHanson
  expiration: 60480000
  token-header: Authorization

minio:
  endpoint: http://nas.zsqnas.icu:9000   # MinIO 控制台端口
  access-key: minio
  secret-key: minio@123
  bucket-name: hellominio